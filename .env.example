# Supabase credentials
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Admin email (case-insensitive). The user with a matching email becomes the ONLY admin.
# If not set, the fallback is user id 1 being treated as admin.
VITE_ADMIN_EMAIL=your.admin@email.com
